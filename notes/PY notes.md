 - isinstance(duration, int) — asks "is duration a whole number?" and gives True/False. The not in front flips it: if it's not a whole
 number, reject. That's what stops "banana".
 
  - name.strip() — .strip() chops spaces off both ends of text. So "   ".strip() becomes "", which means "   ".strip() == "" catches both an
 empty name and a spaces-only name in one check.


if priority not in (1, 2, 3):  

(1,2,3) = TOUPLE
a touple is like an unchanging list


